# ---------------------------------------------------------------------------
# Rust
# ---------------------------------------------------------------------------
# The workspace target directory. It holds the native cdylib/rlib build.
/target
**/*.rs.bk
*.rlib
*.rmeta
*.pdb

# Cargo.lock is intentionally NOT ignored. This crate owns a reproducible
# contract (the ABI and the content hash), so pinning the exact dependency graph
# is part of the build being reproducible.

# ---------------------------------------------------------------------------
# cbindgen
# ---------------------------------------------------------------------------
# include/colander.h is generated by cbindgen but is intentionally tracked: it
# is the header every C caller compiles against. Regenerating it is a commit,
# not a build step. Do not ignore it.

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
# For ctypes bindings built against this library. Cargo builds into target/, so
# a shared object appears only under the paths above; do not add a blanket
# *.so / *.dylib / *.dll rule here.
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# Editors
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# Operating systems
# ---------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
Thumbs.db
Desktop.ini

# ---------------------------------------------------------------------------
# Local tooling caches
# ---------------------------------------------------------------------------
.codegraph/
.atl/
odd/

# ---------------------------------------------------------------------------
# Environment and credentials
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# ---------------------------------------------------------------------------
# Coverage and profiling
# ---------------------------------------------------------------------------
*.profraw
*.profdata
lcov.info
perf.data*
/coverage/
